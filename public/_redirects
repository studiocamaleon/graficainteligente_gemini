# Netlify redirects file
# This ensures all routes are handled by React Router (SPA)
# https://docs.netlify.com/routing/redirects/

# Redirect all requests to index.html for client-side routing
/*    /index.html   200
